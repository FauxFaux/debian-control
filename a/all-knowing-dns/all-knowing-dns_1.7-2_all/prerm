#!/bin/sh
set -e
# Automatically added by dh_installinit/11.3.5
if [ -x "/etc/init.d/all-knowing-dns" ] && [ "$1" = remove ]; then
	invoke-rc.d all-knowing-dns stop || exit 1
fi
# End automatically added section
