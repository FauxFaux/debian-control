#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/anope" ]; then
		update-rc.d anope defaults >/dev/null
		invoke-rc.d anope start || exit $?
	fi
fi
# End automatically added section
