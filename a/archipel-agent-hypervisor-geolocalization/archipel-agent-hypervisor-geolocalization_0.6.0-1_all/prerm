#!/bin/sh
set -e

# Automatically added by dh_python2:
if which pyclean >/dev/null 2>&1; then
	pyclean -p archipel-agent-hypervisor-geolocalization 
else
	dpkg -L archipel-agent-hypervisor-geolocalization | grep \.py$ | while read file
	do
		rm -f "${file}"[co] >/dev/null
  	done
fi

# End automatically added section
