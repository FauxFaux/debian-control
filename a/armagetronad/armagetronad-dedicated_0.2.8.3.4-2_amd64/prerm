#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/armagetronad-dedicated" ] && [ "$1" = remove ]; then
	invoke-rc.d armagetronad-dedicated stop || exit $?
fi
# End automatically added section
