#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/arpon" ]; then
		update-rc.d arpon defaults >/dev/null
		invoke-rc.d arpon start || exit $?
	fi
fi
# End automatically added section
