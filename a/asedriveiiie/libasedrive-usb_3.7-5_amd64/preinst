#!/bin/sh
set -e
# Automatically added by dh_installudev
if [ "$1" = install ] || [ "$1" = upgrade ]; then
	if [ -e "/etc/udev/rules.d/z60_libasedrive-usb.rules" ]; then
		if [ "`md5sum \"/etc/udev/rules.d/z60_libasedrive-usb.rules\" | sed -e \"s/ .*//\"`" = \
		     "`dpkg-query -W -f='${Conffiles}' libasedrive-usb | sed -n -e \"\\\\' /etc/udev/rules.d/z60_libasedrive-usb.rules '{s/ obsolete$//;s/.* //p}\"`" ]
		then
			rm -f "/etc/udev/rules.d/z60_libasedrive-usb.rules"
		fi
	fi
fi
# End automatically added section
