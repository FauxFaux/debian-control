#!/bin/sh
set -e
# Automatically added by installdeb-aspell
dico_class="aspell"
dico_compat="ar-large.compat"

[ -d "/var/lib/$dico_class" ] || mkdir -p "/var/lib/$dico_class"

for i in $dico_compat; do
    >"/var/lib/$dico_class/$i"
done
# End automatically added section
