#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/atopacct" ]; then
	invoke-rc.d atopacct stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/atop" ]; then
	invoke-rc.d atop stop || exit $?
fi
# End automatically added section
