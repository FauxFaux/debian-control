#!/bin/sh
set -e
# Automatically added by dh_installinit/12
if [ -x "/etc/init.d/bacula-director" ] && [ "$1" = remove ]; then
	invoke-rc.d bacula-director stop || exit 1
fi
# End automatically added section
