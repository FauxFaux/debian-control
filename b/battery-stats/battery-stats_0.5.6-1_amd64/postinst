#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/battery-stats" ]; then
		update-rc.d battery-stats defaults >/dev/null
		invoke-rc.d battery-stats start || exit $?
	fi
fi
# End automatically added section
