#!/bin/sh

set -e

if [ -f /usr/share/debconf/confmodule ]; then
    . /usr/share/debconf/confmodule

    set -u

    PKG=bgoffice-dict-downloader

    db_input high $PKG/dict-list || true
    db_go || true
fi



exit 0
