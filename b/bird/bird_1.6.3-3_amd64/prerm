#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/bird6" ] && [ "$1" = remove ]; then
	invoke-rc.d bird6 stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/bird" ] && [ "$1" = remove ]; then
	invoke-rc.d bird stop || exit $?
fi
# End automatically added section
