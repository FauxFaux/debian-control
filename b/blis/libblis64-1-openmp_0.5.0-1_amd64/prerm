#!/bin/sh
set -e
. /usr/share/debconf/confmodule

if [ "$1" != "upgrade" ]; then

    update-alternatives --remove libblas64.so.3-x86_64-linux-gnu \
                        /usr/lib/x86_64-linux-gnu/libblis64.so.1

fi


exit 0
