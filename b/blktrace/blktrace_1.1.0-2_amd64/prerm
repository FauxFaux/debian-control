#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/mountdebugfs" ] && [ "$1" = remove ]; then
	invoke-rc.d mountdebugfs stop || exit $?
fi
# End automatically added section
