#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/bluemon" ]; then
		update-rc.d bluemon defaults >/dev/null
		invoke-rc.d bluemon start || exit $?
	fi
fi
# End automatically added section
