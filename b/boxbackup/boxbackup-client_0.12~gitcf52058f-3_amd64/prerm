#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/boxbackup-client" ] && [ "$1" = remove ]; then
	invoke-rc.d boxbackup-client stop || exit $?
fi
# End automatically added section
