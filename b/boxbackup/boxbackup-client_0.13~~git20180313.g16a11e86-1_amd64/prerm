#!/bin/sh
set -e
# Automatically added by dh_installinit/11.1.5
if [ -x "/etc/init.d/boxbackup-client" ] && [ "$1" = remove ]; then
	invoke-rc.d boxbackup-client stop || exit 1
fi
# End automatically added section
