#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/buildslave" ]; then
	invoke-rc.d buildslave stop || exit $?
fi
# End automatically added section

# Automatically added by dh_python2:
if which pyclean >/dev/null 2>&1; then
	pyclean -p buildbot-slave 
else
	dpkg -L buildbot-slave | grep \.py$ | while read file
	do
		rm -f "${file}"[co] >/dev/null
  	done
fi

# End automatically added section
