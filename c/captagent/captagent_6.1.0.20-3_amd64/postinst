#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/captagent" ]; then
		update-rc.d captagent defaults >/dev/null
		invoke-rc.d captagent start || exit $?
	fi
fi
# End automatically added section
