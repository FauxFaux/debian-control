#!/bin/sh
set -e
# Automatically added by dh_installinit/10.9.2
if [ -x "/etc/init.d/carbon-c-relay" ] && [ "$1" = remove ]; then
	invoke-rc.d carbon-c-relay stop || exit $?
fi
# End automatically added section
