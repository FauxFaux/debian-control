#!/bin/sh
set -e
# Automatically added by dh_installinit/11.3.5
if [ -x "/etc/init.d/ceilometer-agent-compute" ] && [ "$1" = remove ]; then
	invoke-rc.d ceilometer-agent-compute stop || true
fi
# End automatically added section
