#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.9
if [ -x "/etc/init.d/cfengine3" ] && [ "$1" = remove ]; then
	invoke-rc.d cfengine3 stop || exit $?
fi
# End automatically added section
