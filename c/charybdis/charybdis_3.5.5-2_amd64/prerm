#!/bin/sh
set -e
# Automatically added by dh_installinit/10.7.2
if [ -x "/etc/init.d/charybdis" ] && [ "$1" = remove ]; then
	invoke-rc.d charybdis stop || exit $?
fi
# End automatically added section
