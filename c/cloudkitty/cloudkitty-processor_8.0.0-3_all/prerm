#!/bin/sh
set -e
# Automatically added by dh_installinit/12
if [ -x "/etc/init.d/cloudkitty-processor" ] && [ "$1" = remove ]; then
	invoke-rc.d cloudkitty-processor stop || true
fi
# End automatically added section
