#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/clsync" ]; then
		update-rc.d clsync defaults >/dev/null
		invoke-rc.d clsync start || exit $?
	fi
fi
# End automatically added section
