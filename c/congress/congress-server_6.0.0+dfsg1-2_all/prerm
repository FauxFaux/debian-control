#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.5
if [ -x "/etc/init.d/congress-server" ] && [ "$1" = remove ]; then
	invoke-rc.d congress-server stop || true
fi
# End automatically added section
