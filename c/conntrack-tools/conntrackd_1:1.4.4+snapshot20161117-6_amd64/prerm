#!/bin/sh
set -e
# Automatically added by dh_installinit/10.7.2
if [ -x "/etc/init.d/conntrackd" ] && [ "$1" = remove ]; then
	invoke-rc.d conntrackd stop || exit $?
fi
# End automatically added section
