#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/cpqarrayd" ]; then
		update-rc.d cpqarrayd defaults >/dev/null
		invoke-rc.d cpqarrayd start || exit $?
	fi
fi
# End automatically added section
