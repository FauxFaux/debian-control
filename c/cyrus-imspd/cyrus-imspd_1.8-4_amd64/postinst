#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.9
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/cyrus-imspd" ]; then
		update-rc.d cyrus-imspd defaults >/dev/null
		invoke-rc.d cyrus-imspd start || exit $?
	fi
fi
# End automatically added section
