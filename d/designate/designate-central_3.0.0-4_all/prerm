#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/designate-central" ] && [ "$1" = remove ]; then
	invoke-rc.d designate-central stop || true
fi
# End automatically added section
