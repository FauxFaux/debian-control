#!/bin/sh
set -e
# Automatically added by dh_installinit/11.3.5
if [ -x "/etc/init.d/designate-zone-manager" ] && [ "$1" = remove ]; then
	invoke-rc.d designate-zone-manager stop || true
fi
# End automatically added section
