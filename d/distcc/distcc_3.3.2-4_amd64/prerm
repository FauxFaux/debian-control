#!/bin/sh

set -eu

rm -f /usr/lib/distcc/* 2>/dev/null || true

# Automatically added by dh_installinit/12
if [ -x "/etc/init.d/distcc" ] && [ "$1" = remove ]; then
	invoke-rc.d --skip-systemd-native distcc stop || exit 1
fi
# End automatically added section

