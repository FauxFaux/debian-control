#!/bin/sh

# Make sure that we exit on error
set -e

# Automatically added by dh_installinit
if [ -x "/etc/init.d/distributed-net" ]; then
	invoke-rc.d distributed-net stop || exit $?
fi
# End automatically added section

