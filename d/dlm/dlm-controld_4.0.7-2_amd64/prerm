#!/bin/sh
set -e
# Automatically added by dh_installsystemd/11.2.1
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop 'dlm.service' >/dev/null || true
fi
# End automatically added section
