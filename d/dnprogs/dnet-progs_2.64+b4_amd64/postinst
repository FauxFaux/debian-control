#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/dnet-progs" ]; then
		update-rc.d dnet-progs defaults >/dev/null
		invoke-rc.d dnet-progs start || exit $?
	fi
fi
# End automatically added section
