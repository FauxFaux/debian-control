#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/dns2tcp" ]; then
		update-rc.d dns2tcp defaults >/dev/null
		invoke-rc.d dns2tcp start || exit $?
	fi
fi
# End automatically added section
