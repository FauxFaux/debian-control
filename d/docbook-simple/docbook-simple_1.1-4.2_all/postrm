#!/bin/sh
set -e
# Automatically added by dh_installcatalogs
if [ "$1" = "purge" ]; then
	rm -f /etc/sgml/docbook-simple.cat.old
fi
# End automatically added section
# Automatically added by dh_installxmlcatalogs
if [ "$1" = "purge" ]; then
	rm -f /etc/xml/docbook-simple.xml /etc/xml/docbook-simple.xml.old
fi
# End automatically added section
