#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/dynalogin-server" ]; then
		update-rc.d dynalogin-server defaults >/dev/null
		invoke-rc.d dynalogin-server start || exit $?
	fi
fi
# End automatically added section
