#!/bin/sh
set -e

# Automatically added by dhpython:
if which pycompile >/dev/null 2>&1; then
	pycompile -p epylog 
fi

if which pycompile >/dev/null 2>&1; then
	pycompile -p epylog /usr/share/epylog
fi

# End automatically added section
