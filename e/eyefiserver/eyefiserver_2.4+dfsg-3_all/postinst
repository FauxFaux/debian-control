#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/eyefiserver" ]; then
		update-rc.d eyefiserver defaults >/dev/null
		invoke-rc.d eyefiserver start || exit $?
	fi
fi
# End automatically added section
