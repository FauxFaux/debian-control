#!/bin/sh
set -e
# Automatically added by dh_installinit/10.8
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/fastnetmon" ]; then
		update-rc.d fastnetmon defaults >/dev/null
		invoke-rc.d fastnetmon start || exit $?
	fi
fi
# End automatically added section
