#!/bin/sh
set -e
# Automatically added by dh_systemd_start/10.10.3
if [ -d /run/systemd/system ]; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_systemd_enable/10.10.3
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask 'fenrir.service' >/dev/null
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'fenrir.service' >/dev/null
		deb-systemd-helper unmask 'fenrir.service' >/dev/null
	fi
fi
# End automatically added section
