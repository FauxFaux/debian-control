#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/fetch-crl-boot" ] && [ "$1" = remove ]; then
	invoke-rc.d fetch-crl-boot stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/fetch-crl-cron" ] && [ "$1" = remove ]; then
	invoke-rc.d fetch-crl-cron stop || exit $?
fi
# End automatically added section
