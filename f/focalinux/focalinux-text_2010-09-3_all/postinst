#!/bin/sh -e

if [ "$1" = "configure" ]; then
        if [ -d /usr/doc -a -h /usr/doc/focalinux-text -a -d /usr/share/doc/focalinux-text ]; then
                rm -f /usr/doc/focalinux-text
        fi
fi


