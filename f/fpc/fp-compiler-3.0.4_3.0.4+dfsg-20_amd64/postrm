#! /bin/sh

set -e

ACTION=$1

CFG_FILE="/etc/fpc-3.0.4"

# Debhelper code
# Automatically added by dh_installdebconf/11.3.5
if [ "$1" = purge ] && [ -e /usr/share/debconf/confmodule ]; then
	. /usr/share/debconf/confmodule
	db_purge
fi
# End automatically added section


if test "${ACTION}" = "purge"
then
	rm -f  "${CFG_FILE}".cfg
	rm -f  "${CFG_FILE}".bak
fi
