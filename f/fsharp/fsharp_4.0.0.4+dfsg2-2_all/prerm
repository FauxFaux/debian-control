#!/bin/sh
set -e
# Automatically added by dh_installcliframework
if [ "$1" = "remove" ] || [ "$1" = "upgrade" ] && [ -x /usr/share/cli-common/framework-package-remove ]; then
	/usr/share/cli-common/framework-package-remove fsharp
fi
# End automatically added section
