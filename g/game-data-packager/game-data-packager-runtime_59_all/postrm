#!/bin/sh
set -e
# Automatically added by dh_apparmor/2.12-4
if [ "$1" = "purge" ] && ! [ -e "/etc/apparmor.d/usr.lib.unreal" ] ; then
    rm -f "/etc/apparmor.d/disable/usr.lib.unreal" || true
    rm -f "/etc/apparmor.d/force-complain/usr.lib.unreal" || true
    rm -f "/etc/apparmor.d/local/usr.lib.unreal" || true
    rmdir /etc/apparmor.d/disable 2>/dev/null || true
    rmdir /etc/apparmor.d/local   2>/dev/null || true
    rmdir /etc/apparmor.d         2>/dev/null || true
fi
# End automatically added section
