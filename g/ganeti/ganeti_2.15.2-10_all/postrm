#!/bin/sh
# postrm script for ganeti
#

set -e

# summary of how this script can be called:
#        * <postrm> `remove'
#        * <postrm> `purge'
#        * <old-postrm> `upgrade' <new-version>
#        * <new-postrm> `failed-upgrade' <old-version>
#        * <new-postrm> `abort-install'
#        * <new-postrm> `abort-install' <old-version>
#        * <new-postrm> `abort-upgrade' <old-version>
#        * <disappearer's-postrm> `disappear' <overwriter>
#          <overwriter-version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package


case "$1" in
    purge)
	# Users
	deluser --quiet --system "gnt-confd" || true
	deluser --quiet --system "gnt-masterd" || true
	deluser --quiet --system "gnt-masterd" || true
	deluser --quiet --system "gnt-masterd" || true
	deluser --quiet --system "gnt-rapi" || true

	# Groups
	delgroup --quiet --system "gnt-admin" || true
	delgroup --quiet --system "gnt-confd" || true
	delgroup --quiet --system "gnt-daemons" || true
	delgroup --quiet --system "gnt-luxid" || true
	delgroup --quiet --system "gnt-masterd" || true
	delgroup --quiet --system "gnt-rapi" || true

    ;;
    remove)
	rm -f /etc/ganeti/share /etc/ganeti/lib
    ;;
    remove|upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
    ;;

    *)
        echo "postrm called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

# Automatically added by dh_installinit/10.7.2
if [ "$1" = "purge" ] ; then
	update-rc.d ganeti remove >/dev/null
fi


# In case this system is running systemd, we make systemd reload the unit files
# to pick up changes.
if [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section


exit 0
~                                              
