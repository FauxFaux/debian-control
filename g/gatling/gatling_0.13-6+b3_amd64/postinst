#!/bin/sh

set -e

if [ "$1" = configure ] ; then
	if [ ! -e /var/www/default/index.* ] ; then
		mkdir -p /var/www/default
		cp /usr/share/gatling/default-site/index.html /var/www/default/index.html
	fi
fi

# Automatically added by dh_installinit/11.4.1
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
	if [ -x "/etc/init.d/gatling" ]; then
		update-rc.d gatling defaults >/dev/null
		invoke-rc.d gatling start || exit 1
	fi
fi
# End automatically added section


exit 0

