#! /bin/sh -e

case "$1" in
    configure)
	docdir=/usr/share/doc/libgcj16
	if [ -d $docdir ] && [ ! -h $docdir ]; then
	  rm -rf /usr/share/doc/libgcj16
	  ln -s gcj-5-base /usr/share/doc/libgcj16
	fi
esac


