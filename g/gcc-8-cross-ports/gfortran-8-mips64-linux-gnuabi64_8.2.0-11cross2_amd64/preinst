#!/bin/sh

set -e

if [ "$1" = "upgrade" ] || [ "$1" = "configure" ]; then
    update-alternatives --quiet --remove mips64-linux-gnuabi64-gfortran /usr/bin/mips64-linux-gnuabi64-gfortran-8
fi



exit 0
