#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/gcpegg" ]; then
		update-rc.d gcpegg defaults >/dev/null
		invoke-rc.d gcpegg start || exit $?
	fi
fi
# End automatically added section
