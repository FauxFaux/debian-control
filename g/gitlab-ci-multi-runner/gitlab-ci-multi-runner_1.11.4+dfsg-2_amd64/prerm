#!/bin/sh
set -e
# Automatically added by dh_installinit/10.9.2
if [ -x "/etc/init.d/gitlab-ci-multi-runner" ] && [ "$1" = remove ]; then
	invoke-rc.d gitlab-ci-multi-runner stop || exit $?
fi
# End automatically added section
