#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5.1
if [ -x "/etc/init.d/glance-api" ] && [ "$1" = remove ]; then
	invoke-rc.d glance-api stop || true
fi
# End automatically added section
