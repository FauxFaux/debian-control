#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.5
if [ -x "/etc/init.d/globus-gridftp-sshftp" ]; then
	invoke-rc.d globus-gridftp-sshftp stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit/10.10.5
if [ -x "/etc/init.d/globus-gridftp-server" ]; then
	invoke-rc.d globus-gridftp-server stop || exit $?
fi
# End automatically added section
