#!/bin/sh

set -e

if [ "$1" != "upgrade" ]; then
  if [ "mpich" != "serial" ]; then
    update-alternatives \
	--remove h5pcc /usr/bin/h5pcc.mpich
  fi
  update-alternatives \
	--remove hdf5.pc /usr/lib/x86_64-linux-gnu/pkgconfig/hdf5-mpich.pc
fi



exit 0
