#!/bin/sh
set -e
# Automatically added by installdeb-aspell/UNDECLARED
dico_class="aspell"
dico_compat="de-1901.compat"

[ -d "/var/lib/$dico_class" ] || mkdir -p "/var/lib/$dico_class"

for i in $dico_compat; do
    >"/var/lib/$dico_class/$i"
done
# End automatically added section
