#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/i8kmon" ]; then
		update-rc.d i8kmon defaults >/dev/null
		invoke-rc.d i8kmon start || exit $?
	fi
fi
# End automatically added section
