#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/ikiwiki-hosting-web" ] && [ "$1" = remove ]; then
	invoke-rc.d ikiwiki-hosting-web stop || exit $?
fi
# End automatically added section
