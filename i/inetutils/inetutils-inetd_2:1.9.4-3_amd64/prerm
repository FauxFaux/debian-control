#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.5
if [ -x "/etc/init.d/inetutils-inetd" ] && [ "$1" = remove ]; then
	invoke-rc.d inetutils-inetd stop || exit $?
fi
# End automatically added section
