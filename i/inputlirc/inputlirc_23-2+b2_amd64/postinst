#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/inputlirc" ]; then
		update-rc.d inputlirc defaults >/dev/null
		invoke-rc.d inputlirc start || exit $?
	fi
fi
# End automatically added section
