#!/bin/sh
set -e
# Automatically added by dh_installinit/11.1.2
if [ -x "/etc/init.d/rtcw-server" ] && [ "$1" = remove ]; then
	invoke-rc.d rtcw-server stop || exit 1
fi
# End automatically added section
