#!/bin/sh

set -e

ipmievd_initd_failed() {
    :
}

# Automatically added by dh_installinit/10.10.7
if [ -x "/etc/init.d/ipmievd" ] && [ "$1" = remove ]; then
	invoke-rc.d ipmievd stop || ipmievd_initd_failed
fi
# End automatically added section

