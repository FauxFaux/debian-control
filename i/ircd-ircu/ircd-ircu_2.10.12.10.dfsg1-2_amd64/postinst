#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/ircd-ircu" ]; then
		update-rc.d ircd-ircu defaults >/dev/null
		invoke-rc.d ircd-ircu start || exit $?
	fi
fi
# End automatically added section
