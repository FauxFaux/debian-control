#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/isakmpd" ]; then
		update-rc.d isakmpd defaults >/dev/null
		invoke-rc.d isakmpd start || exit $?
	fi
fi
# End automatically added section
