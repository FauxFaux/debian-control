#!/bin/sh

set -e

if [ "${1}" = "configure" ] || [ "${1}" = "reconfigure" ]; then
	touch /etc/ivulncheck/ivulncheck-agent
	chmod 640 /etc/ivulncheck/ivulncheck-agent || true
	chown root:root /etc/ivulncheck/ivulncheck-agent || true

fi



exit 0

