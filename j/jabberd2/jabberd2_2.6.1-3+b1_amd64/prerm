#!/bin/sh
set -e
# Automatically added by dh_installsystemd/11.2.1
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop 'jabberd-c2s.service' 'jabberd-router.service' 'jabberd-s2s.service' 'jabberd-sm.service' >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/11.2.1
if [ -x "/etc/init.d/jabberd2" ] && [ "$1" = remove ]; then
	invoke-rc.d jabberd2 stop || exit 1
fi
# End automatically added section
