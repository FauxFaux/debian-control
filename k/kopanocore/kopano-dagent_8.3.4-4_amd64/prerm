#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.2
if [ -x "/etc/init.d/kopano-dagent" ] && [ "$1" = remove ]; then
	invoke-rc.d kopano-dagent stop || exit $?
fi
# End automatically added section
