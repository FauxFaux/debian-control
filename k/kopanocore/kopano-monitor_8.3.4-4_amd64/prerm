#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.2
if [ -x "/etc/init.d/kopano-monitor" ] && [ "$1" = remove ]; then
	invoke-rc.d kopano-monitor stop || exit $?
fi
# End automatically added section
