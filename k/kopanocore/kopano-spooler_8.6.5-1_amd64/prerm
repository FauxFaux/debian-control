#!/bin/sh
set -e
# Automatically added by dh_installinit/11.3.5
if [ -x "/etc/init.d/kopano-spooler" ] && [ "$1" = remove ]; then
	invoke-rc.d kopano-spooler stop || exit 1
fi
# End automatically added section
