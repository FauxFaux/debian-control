#!/bin/sh
# dpkg does not replace symlinks with directories and vice versa, therefore
# maintainer scripts need to be used. 
set -e

if [ -L /usr/share/doc/kraptor ] ; then
  rm -f /usr/share/doc/kraptor
fi


