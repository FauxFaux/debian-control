#!/bin/sh
set -e
# Automatically added by dh_installinit/11.2.1
if [ -x "/etc/init.d/kubelet" ] && [ "$1" = remove ]; then
	invoke-rc.d kubelet stop || exit 1
fi
# End automatically added section
# Automatically added by dh_installinit/11.2.1
if [ -x "/etc/init.d/kube-proxy" ] && [ "$1" = remove ]; then
	invoke-rc.d kube-proxy stop || exit 1
fi
# End automatically added section
