#!/bin/sh
set -e

# Automatically added by dh_python3:
if which py3compile >/dev/null 2>&1; then
	py3compile -p lava-common -V 3.5-
fi
if which pypy3compile >/dev/null 2>&1; then
	pypy3compile -p lava-common -V 3.5- || true
fi

if which py3compile >/dev/null 2>&1; then
	py3compile -p lava-common /usr/share/lava-common -V 3.5-
fi
if which pypy3compile >/dev/null 2>&1; then
	pypy3compile -p lava-common /usr/share/lava-common -V 3.5- || true
fi

# End automatically added section
