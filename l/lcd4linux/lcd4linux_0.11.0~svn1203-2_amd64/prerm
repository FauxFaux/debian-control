#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.9
if [ -x "/etc/init.d/lcd4linux" ] && [ "$1" = remove ]; then
	invoke-rc.d lcd4linux stop || exit $?
fi
# End automatically added section
