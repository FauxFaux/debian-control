#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5.3
if [ "$1" = "purge" ] ; then
	update-rc.d LCDd remove >/dev/null
fi
# End automatically added section
# Automatically added by dh_installdebconf/11.5.3
if [ "$1" = purge ] && [ -e /usr/share/debconf/confmodule ]; then
	. /usr/share/debconf/confmodule
	db_purge
fi
# End automatically added section
# Automatically added by dh_cme_upgrade/UNDECLARED
# Purge configuration files handled by cme

if [ "$1" = purge ]
then
    echo "Purging cme files: '/etc/LCDd.conf*'"
    rm -rf /etc/LCDd.conf*
fi
# End automatically added section
