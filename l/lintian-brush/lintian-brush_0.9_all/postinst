#!/bin/sh
set -e

# Automatically added by dh_python3:
if which py3compile >/dev/null 2>&1; then
	py3compile -p lintian-brush 
fi
if which pypy3compile >/dev/null 2>&1; then
	pypy3compile -p lintian-brush  || true
fi

if which py3compile >/dev/null 2>&1; then
	py3compile -p lintian-brush /usr/share/lintian-brush
fi
if which pypy3compile >/dev/null 2>&1; then
	pypy3compile -p lintian-brush /usr/share/lintian-brush || true
fi

# End automatically added section
