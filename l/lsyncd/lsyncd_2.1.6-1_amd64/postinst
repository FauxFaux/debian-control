#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/lsyncd" ]; then
		update-rc.d lsyncd defaults >/dev/null
		invoke-rc.d lsyncd start || exit $?
	fi
fi
# End automatically added section
