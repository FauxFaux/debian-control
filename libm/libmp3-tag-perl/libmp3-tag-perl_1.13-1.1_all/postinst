#!/bin/sh

set -e

if [ "$1" = "configure" ]
then
    if [ -n "$2" ] && dpkg --compare-versions "$2" lt "0.9709-1"
    then
        if which mktexlsr >/dev/null
	then
	    mktexlsr /usr/share/texmf
	fi
    fi
fi



