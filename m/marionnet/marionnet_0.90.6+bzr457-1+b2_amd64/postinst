#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/marionnet" ]; then
		update-rc.d marionnet defaults >/dev/null
		invoke-rc.d marionnet start || exit $?
	fi
fi
# End automatically added section
