#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/minetest-server" ] && [ "$1" = remove ]; then
	invoke-rc.d minetest-server stop || exit $?
fi
# End automatically added section
