#!/bin/sh

set -e


if [ "$1" = "purge" ] ; then
    rm -r /var/games/minetest-server
    rm -r /var/log/minetest
    rmdir --ignore-fail-on-non-empty /var/games
fi

# Automatically added by dh_installinit/11.3.2
if [ "$1" = "purge" ] ; then
	update-rc.d minetest-server remove >/dev/null
fi


# In case this system is running systemd, we make systemd reload the unit files
# to pick up changes.
if [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installsystemd/11.3.2
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask 'minetest-server.service' >/dev/null || true
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'minetest-server.service' >/dev/null || true
		deb-systemd-helper unmask 'minetest-server.service' >/dev/null || true
	fi
fi
# End automatically added section

