#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/minicoredumper" ] && [ "$1" = remove ]; then
	invoke-rc.d minicoredumper stop || exit $?
fi
# End automatically added section
