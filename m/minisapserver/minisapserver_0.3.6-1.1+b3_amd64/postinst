#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/minisapserver" ]; then
		update-rc.d minisapserver defaults >/dev/null
		invoke-rc.d minisapserver start || exit $?
	fi
fi
# End automatically added section
