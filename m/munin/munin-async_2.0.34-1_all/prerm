#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.2
if [ -x "/etc/init.d/munin-async" ] && [ "$1" = remove ]; then
	invoke-rc.d munin-async stop || exit $?
fi
# End automatically added section
