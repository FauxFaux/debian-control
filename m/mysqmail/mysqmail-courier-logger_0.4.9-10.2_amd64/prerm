#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/mysqmail-courier-logger" ]; then
	invoke-rc.d mysqmail-courier-logger stop || exit $?
fi
# End automatically added section
