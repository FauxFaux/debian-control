#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/mysqmail-dovecot-logger" ]; then
	invoke-rc.d mysqmail-dovecot-logger stop || exit $?
fi
# End automatically added section
