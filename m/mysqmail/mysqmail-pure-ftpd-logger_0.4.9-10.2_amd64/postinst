#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/mysqmail-pure-ftpd-logger" ]; then
		update-rc.d mysqmail-pure-ftpd-logger defaults >/dev/null
		invoke-rc.d mysqmail-pure-ftpd-logger start || exit $?
	fi
fi
# End automatically added section
