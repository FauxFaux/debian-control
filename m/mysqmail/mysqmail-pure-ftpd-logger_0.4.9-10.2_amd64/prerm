#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/mysqmail-pure-ftpd-logger" ]; then
	invoke-rc.d mysqmail-pure-ftpd-logger stop || exit $?
fi
# End automatically added section
