#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/mythtv-status" ] && [ "$1" = remove ]; then
	invoke-rc.d mythtv-status stop || exit $?
fi
# End automatically added section
