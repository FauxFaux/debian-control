#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/ndppd" ]; then
		update-rc.d ndppd defaults >/dev/null
		invoke-rc.d ndppd start || exit $?
	fi
fi
# End automatically added section
