#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/bootparamd" ]; then
		update-rc.d bootparamd defaults >/dev/null
		invoke-rc.d bootparamd start || exit $?
	fi
fi
# End automatically added section
