#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/neutron-lbaasv2-agent" ] && [ "$1" = remove ]; then
	invoke-rc.d neutron-lbaasv2-agent stop || true
fi
# End automatically added section
