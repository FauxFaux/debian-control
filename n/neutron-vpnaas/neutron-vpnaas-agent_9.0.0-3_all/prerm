#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/neutron-vpnaas-agent" ] && [ "$1" = remove ]; then
	invoke-rc.d neutron-vpnaas-agent stop || true
fi
# End automatically added section
