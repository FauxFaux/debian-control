#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5.3
if [ -x "/etc/init.d/neutron-rpc-server" ] && [ "$1" = remove ]; then
	invoke-rc.d neutron-rpc-server stop || true
fi
# End automatically added section
