#!/bin/sh
set -e
# Automatically added by dh_cligacpolicy
if [ "$1" = "remove" ] ||  [ "$1" = "upgrade" ] && [ -x /usr/share/cli-common/policy-remove ]; then
	/usr/share/cli-common/policy-remove Newtonsoft.Json 5.0
fi
# End automatically added section
