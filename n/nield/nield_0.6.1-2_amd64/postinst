#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/nield" ]; then
		update-rc.d nield defaults >/dev/null
		invoke-rc.d nield start || exit $?
	fi
fi
# End automatically added section
