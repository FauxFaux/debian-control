#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/gridftpd" ]; then
		update-rc.d gridftpd defaults >/dev/null
		invoke-rc.d gridftpd start || exit $?
	fi
fi
# End automatically added section
