#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/arched" ]; then
		update-rc.d arched defaults >/dev/null
		invoke-rc.d arched start || exit $?
	fi
fi
# End automatically added section
