#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/nova-consoleauth" ] && [ "$1" = remove ]; then
	invoke-rc.d nova-consoleauth stop || true
fi
# End automatically added section
