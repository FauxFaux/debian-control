#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5.3
if [ -x "/etc/init.d/nova-xenvncproxy" ] && [ "$1" = remove ]; then
	invoke-rc.d nova-xenvncproxy stop || exit 1
fi
# End automatically added section
# Automatically added by dh_installinit/11.5.3
if [ -x "/etc/init.d/nova-spicehtml5proxy" ] && [ "$1" = remove ]; then
	invoke-rc.d nova-spicehtml5proxy stop || exit 1
fi
# End automatically added section
# Automatically added by dh_installinit/11.5.3
if [ -x "/etc/init.d/nova-novncproxy" ] && [ "$1" = remove ]; then
	invoke-rc.d nova-novncproxy stop || exit 1
fi
# End automatically added section
