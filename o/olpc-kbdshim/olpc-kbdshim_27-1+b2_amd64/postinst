#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/olpc-kbdshim" ]; then
		update-rc.d olpc-kbdshim defaults >/dev/null
		invoke-rc.d olpc-kbdshim start || exit $?
	fi
fi
# End automatically added section
