#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/olpc-powerd" ]; then
		update-rc.d olpc-powerd defaults >/dev/null
		invoke-rc.d olpc-powerd start || exit $?
	fi
fi
# End automatically added section
