#!/bin/sh

set -e

case "${1}" in
	remove)
		rm -rf /var/lib/package-tracker
		;;

	purge)
		rm -rf /var/log/package-tracker
		;;

	upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)

		;;

	*)
		echo "postrm called with unknown argument \`$1'" >&2
		exit 1
		;;
esac



exit 0
