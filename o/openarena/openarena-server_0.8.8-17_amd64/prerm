#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/openarena-server" ] && [ "$1" = remove ]; then
	invoke-rc.d openarena-server stop || exit $?
fi
# End automatically added section
