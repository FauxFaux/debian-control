#!/bin/sh

set -e

if [ "$1" = "purge" ]
then
	rm -rf /var/cache/openrocket/* /usr/bin/openrocket
fi

# Automatically added by dh_installmenu
if [ -x "`which update-menus 2>/dev/null`" ]; then update-menus ; fi
# End automatically added section


exit 0

