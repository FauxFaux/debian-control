#!/bin/sh

set -e

# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/opensm" ]; then
		update-rc.d opensm defaults >/dev/null
		invoke-rc.d opensm start || exit $?
	fi
fi
# End automatically added section

