#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/trove-conductor" ] && [ "$1" = remove ]; then
	invoke-rc.d trove-conductor stop || true
fi
# End automatically added section
