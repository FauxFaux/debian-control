#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5.3
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
	if [ -x "/etc/init.d/osmo-ggsn" ]; then
		update-rc.d osmo-ggsn defaults >/dev/null
		if [ -n "$2" ]; then
			_dh_action=restart
		else
			_dh_action=start
		fi
		invoke-rc.d osmo-ggsn $_dh_action || exit 1
	fi
fi
# End automatically added section
# Automatically added by dh_installsystemd/11.5.3
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ] || [ "$1" = "abort-deconfigure" ] || [ "$1" = "abort-remove" ] ; then
	if deb-systemd-helper debian-installed 'osmo-ggsn.service'; then
		# This will only remove masks created by d-s-h on package removal.
		deb-systemd-helper unmask 'osmo-ggsn.service' >/dev/null || true

		if deb-systemd-helper --quiet was-enabled 'osmo-ggsn.service'; then
			# Create new symlinks, if any.
			deb-systemd-helper enable 'osmo-ggsn.service' >/dev/null || true
		fi
	fi

	# Update the statefile to add new symlinks (if any), which need to be cleaned
	# up on purge. Also remove old symlinks.
	deb-systemd-helper update-state 'osmo-ggsn.service' >/dev/null || true
fi
# End automatically added section
