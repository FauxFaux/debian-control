#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/oss-preserve" ]; then
		update-rc.d oss-preserve defaults >/dev/null
		invoke-rc.d oss-preserve start || exit $?
	fi
fi
# End automatically added section
