#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/perlbal" ]; then
	update-rc.d perlbal defaults >/dev/null
	invoke-rc.d perlbal start || exit $?
fi
# End automatically added section
