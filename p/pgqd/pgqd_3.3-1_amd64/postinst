#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.5
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/pgqd" ]; then
		update-rc.d pgqd defaults >/dev/null
		invoke-rc.d pgqd start || exit $?
	fi
fi
# End automatically added section
