#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/policyd-weight" ] && [ "$1" = remove ]; then
	invoke-rc.d policyd-weight stop || exit $?
fi
# End automatically added section
