#!/bin/sh

set -e

init_eh () {
	:
}

# Automatically added by dh_installinit/11.3.2
if [ -x "/etc/init.d/pommed" ] && [ "$1" = remove ]; then
	invoke-rc.d pommed stop || init_eh
fi
# End automatically added section

