#! /bin/sh
set -e

# Source debconf library.
. /usr/share/debconf/confmodule

if [ "$1" = configure ]
then
    db_get shared/news/server

    # Write the anser to /etc/news/server:
    echo "$RET" > /etc/news/server
    chmod 644 /etc/news/server
fi


