#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/prelude-lml" ]; then
		update-rc.d prelude-lml defaults >/dev/null
		invoke-rc.d prelude-lml start || exit $?
	fi
fi
# End automatically added section
