#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5.3
if [ -x "/etc/init.d/prometheus-apache-exporter" ] && [ "$1" = remove ]; then
	invoke-rc.d prometheus-apache-exporter stop || exit 1
fi
# End automatically added section
