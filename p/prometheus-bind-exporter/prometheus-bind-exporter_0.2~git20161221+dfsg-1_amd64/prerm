#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/prometheus-bind-exporter" ] && [ "$1" = remove ]; then
	invoke-rc.d prometheus-bind-exporter stop || exit $?
fi
# End automatically added section
