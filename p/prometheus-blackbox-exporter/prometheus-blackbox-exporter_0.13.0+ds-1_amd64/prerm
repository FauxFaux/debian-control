#!/bin/sh
set -e
# Automatically added by dh_installinit/12
if [ -x "/etc/init.d/prometheus-blackbox-exporter" ] && [ "$1" = remove ]; then
	invoke-rc.d prometheus-blackbox-exporter stop || exit 1
fi
# End automatically added section
