#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/prometheus-blackbox-exporter" ]; then
	invoke-rc.d prometheus-blackbox-exporter stop || exit $?
fi
# End automatically added section
