#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/prometheus-mongodb-exporter" ]; then
	invoke-rc.d prometheus-mongodb-exporter stop || exit $?
fi
# End automatically added section
