#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/prometheus-mysqld-exporter" ]; then
	invoke-rc.d prometheus-mysqld-exporter stop || exit $?
fi
# End automatically added section
