#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/prometheus-varnish-exporter" ]; then
	invoke-rc.d prometheus-varnish-exporter stop || exit $?
fi
# End automatically added section
