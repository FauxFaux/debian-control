#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.3
if [ -x "/etc/init.d/prometheus" ] && [ "$1" = remove ]; then
	invoke-rc.d prometheus stop || exit $?
fi
# End automatically added section
