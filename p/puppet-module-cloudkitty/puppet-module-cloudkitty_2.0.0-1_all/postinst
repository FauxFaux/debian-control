#!/bin/sh

set -e

if [ "${1}" = "configure" ] ; then
	update-alternatives --install /usr/share/puppet/modules/cloudkitty puppet-module-cloudkitty /usr/share/puppet/modules.available/cloudkitty 500
fi



exit 0
