#!/bin/sh

set -e

if [ "${1}" = "configure" ] ; then
	update-alternatives --install /usr/share/puppet/modules/wait_for puppet-module-wait-for /usr/share/puppet/modules.available/heini-wait-for 500
fi



exit 0
