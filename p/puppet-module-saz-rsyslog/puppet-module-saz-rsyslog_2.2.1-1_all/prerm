#!/bin/sh

set -e

if [ "${1}" = "remove" ] || [ "${1}" = "upgrade" ] || [ "${1}" = "deconfigure" ] ; then
	update-alternatives --remove puppet-module-rsyslog /usr/share/puppet/modules.available/saz-rsyslog
fi



exit 0
