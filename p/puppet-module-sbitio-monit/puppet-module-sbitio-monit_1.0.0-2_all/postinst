#!/bin/sh

set -e

if [ "${1}" = "configure" ] ; then
	update-alternatives --install /usr/share/puppet/modules/monit puppet-module-monit /usr/share/puppet/modules.available/sbitio-monit 500
fi



exit 0
