#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/puppet-master" ] && [ "$1" = remove ]; then
	invoke-rc.d puppet-master stop || exit $?
fi
# End automatically added section
