#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/pybit-watcher" ]; then
		update-rc.d pybit-watcher defaults >/dev/null
		invoke-rc.d pybit-watcher start || exit $?
	fi
fi
# End automatically added section
