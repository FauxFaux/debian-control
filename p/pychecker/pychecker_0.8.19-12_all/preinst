#!/bin/sh

set -e

# Remove old files left behind by previous versions of package
if [ -d /usr/lib/site-python/pychecker ]; then
   rm -f /usr/lib/site-python/pychecker/*.pyo
   rm -f /usr/lib/site-python/pychecker/*.pyc
fi



