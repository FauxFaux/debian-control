#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5
if [ -x "/etc/init.d/rabbitmq-server" ] && [ "$1" = remove ]; then
	invoke-rc.d rabbitmq-server stop || exit 1
fi
# End automatically added section
