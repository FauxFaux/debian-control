#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/randomsound" ]; then
		update-rc.d randomsound defaults >/dev/null
		invoke-rc.d randomsound start || exit $?
	fi
fi
# End automatically added section
