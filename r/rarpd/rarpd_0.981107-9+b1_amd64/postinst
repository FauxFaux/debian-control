#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/rarpd" ]; then
		update-rc.d rarpd defaults >/dev/null
		invoke-rc.d rarpd start || exit $?
	fi
fi
# End automatically added section
