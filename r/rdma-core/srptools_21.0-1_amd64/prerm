#!/bin/sh
set -e
# Automatically added by dh_installsystemd/11.5.3
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop 'srp_daemon.service' >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/11.5.3
if [ -x "/etc/init.d/srptools" ] && [ "$1" = remove ]; then
	invoke-rc.d srptools stop || exit 1
fi
# End automatically added section
