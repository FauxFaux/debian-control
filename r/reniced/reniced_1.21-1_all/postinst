#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/reniced" ]; then
		update-rc.d reniced defaults >/dev/null
		invoke-rc.d reniced start || exit $?
	fi
fi
# End automatically added section
