#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/restartd" ]; then
		update-rc.d restartd defaults >/dev/null
		invoke-rc.d restartd start || exit $?
	fi
fi
# End automatically added section
