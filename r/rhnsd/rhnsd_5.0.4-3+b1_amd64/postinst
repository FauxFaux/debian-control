#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/rhnsd" ]; then
		update-rc.d rhnsd defaults >/dev/null
		invoke-rc.d rhnsd start || exit $?
	fi
fi
# End automatically added section
