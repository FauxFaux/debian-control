#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/rinetd" ]; then
		update-rc.d rinetd defaults >/dev/null
		invoke-rc.d rinetd start || exit $?
	fi
fi
# End automatically added section
