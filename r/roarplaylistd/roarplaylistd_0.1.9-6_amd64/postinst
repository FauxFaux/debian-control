#!/bin/sh

set -e

case "$1" in
	configure)
		adduser --system --ingroup audio --home /var/lib/roarplaylistd --quiet roarplaylistd || true
	;;
	abort-upgrade|abort-remove|abort-deconfigure)
	;;
	*)
		echo "postinst called with unknown argument \`$1'" >&2
		exit 1
	;;
esac

# Automatically added by dh_installinit/10.10.5
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/roarplaylistd" ]; then
		update-rc.d roarplaylistd defaults >/dev/null
		invoke-rc.d roarplaylistd start || exit $?
	fi
fi
# End automatically added section


exit 0
