#!/bin/sh

set -e

pkg=rplayd

if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/$pkg ]; then
  rm -f /usr/doc/$pkg
fi

# Automatically added by dh_installinit
if [ -x "/etc/init.d/rplay" ]; then
	invoke-rc.d rplay stop || exit $?
fi
# End automatically added section

