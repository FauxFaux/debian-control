#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/rrdcollect" ]; then
		update-rc.d rrdcollect defaults >/dev/null
		invoke-rc.d rrdcollect start || exit $?
	fi
fi
# End automatically added section
