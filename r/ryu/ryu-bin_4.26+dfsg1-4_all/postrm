#!/bin/sh

set -e

if [ "${1}" = "purge" ] ; then
	rm -rf /var/lib/ryu
	rm -rf /var/log/ryu
fi

# Automatically added by dh_installinit/12
if [ "$1" = "purge" ] ; then
	update-rc.d ryu remove >/dev/null
fi
# End automatically added section


exit 0
