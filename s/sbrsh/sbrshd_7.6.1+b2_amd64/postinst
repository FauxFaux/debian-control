#!/bin/sh

set -e

# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/sbrshd" ]; then
		update-rc.d sbrshd defaults >/dev/null
		invoke-rc.d sbrshd start || exit $?
	fi
fi
# End automatically added section


exit 0
