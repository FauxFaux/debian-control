#!/bin/sh
set -e
# Automatically added by dh_installsystemd/11.3.1
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop 'scanbm.socket' >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/11.3.1
if [ -x "/etc/init.d/scanbd" ] && [ "$1" = remove ]; then
	invoke-rc.d scanbd stop || exit 1
fi
# End automatically added section
