#!/bin/sh
set -e
# Automatically added by dh_installinit/11.3.5
if [ -x "/etc/init.d/shairport-sync" ] && [ "$1" = remove ]; then
	invoke-rc.d shairport-sync stop || exit 1
fi
# End automatically added section
