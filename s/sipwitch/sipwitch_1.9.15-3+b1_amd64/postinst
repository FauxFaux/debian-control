#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/sipwitch" ]; then
		update-rc.d sipwitch defaults >/dev/null
		invoke-rc.d sipwitch start || exit $?
	fi
fi
# End automatically added section
