#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/slashem-common" ] && [ "$1" = remove ]; then
	invoke-rc.d slashem-common stop || exit $?
fi
# End automatically added section
