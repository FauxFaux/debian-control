#!/bin/sh

set -e

if [ "$1" = configure ] && [ -z "$2" ]; then
	chown postgres:postgres /var/log/slony1/
	chmod u=rwx,g=rwx,o=rxt /var/log/slony1/
fi

# Automatically added by dh_installinit/10.9
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/slony1" ]; then
		update-rc.d slony1 defaults >/dev/null
		invoke-rc.d slony1 start || exit $?
	fi
fi
# End automatically added section

