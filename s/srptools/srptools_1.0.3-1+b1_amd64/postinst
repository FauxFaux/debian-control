#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/srptools" ]; then
		update-rc.d srptools defaults >/dev/null
		invoke-rc.d srptools start || exit $?
	fi
fi
# End automatically added section
