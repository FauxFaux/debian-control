#!/bin/sh
set -e
# Automatically added by dh_apparmor/2.11.1-4
if [ "$1" = "purge" ] && ! [ -e "/etc/apparmor.d/usr.sbin.swanctl" ] ; then
    rm -f "/etc/apparmor.d/disable/usr.sbin.swanctl" || true
    rm -f "/etc/apparmor.d/force-complain/usr.sbin.swanctl" || true
    rm -f "/etc/apparmor.d/local/usr.sbin.swanctl" || true
    rmdir /etc/apparmor.d/disable 2>/dev/null || true
    rmdir /etc/apparmor.d/local   2>/dev/null || true
    rmdir /etc/apparmor.d         2>/dev/null || true
fi
# End automatically added section
