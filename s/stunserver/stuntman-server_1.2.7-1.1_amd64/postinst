#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/stuntman-server" ]; then
		update-rc.d stuntman-server defaults >/dev/null
		invoke-rc.d stuntman-server start || exit $?
	fi
fi
# End automatically added section
