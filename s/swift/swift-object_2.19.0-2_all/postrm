#!/bin/sh
set -e
# Automatically added by dh_installinit/11.3.5
if [ "$1" = "purge" ] ; then
	update-rc.d swift-object remove >/dev/null
fi


# In case this system is running systemd, we make systemd reload the unit files
# to pick up changes.
if [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/11.3.5
if [ "$1" = "purge" ] ; then
	update-rc.d swift-object-updater remove >/dev/null
fi


# In case this system is running systemd, we make systemd reload the unit files
# to pick up changes.
if [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/11.3.5
if [ "$1" = "purge" ] ; then
	update-rc.d swift-object-replicator remove >/dev/null
fi


# In case this system is running systemd, we make systemd reload the unit files
# to pick up changes.
if [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/11.3.5
if [ "$1" = "purge" ] ; then
	update-rc.d swift-object-reconstructor remove >/dev/null
fi


# In case this system is running systemd, we make systemd reload the unit files
# to pick up changes.
if [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/11.3.5
if [ "$1" = "purge" ] ; then
	update-rc.d swift-object-auditor remove >/dev/null
fi


# In case this system is running systemd, we make systemd reload the unit files
# to pick up changes.
if [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_systemd_enable/11.3.5
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' 'swift-object-updater.service' 'swift-object.service' >/dev/null || true
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' 'swift-object-updater.service' 'swift-object.service' >/dev/null || true
		deb-systemd-helper unmask 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' 'swift-object-updater.service' 'swift-object.service' >/dev/null || true
	fi
fi
# End automatically added section
# Automatically added by dh_systemd_enable/11.3.5
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' 'swift-object-updater.service' >/dev/null || true
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' 'swift-object-updater.service' >/dev/null || true
		deb-systemd-helper unmask 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' 'swift-object-updater.service' >/dev/null || true
	fi
fi
# End automatically added section
# Automatically added by dh_systemd_enable/11.3.5
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' >/dev/null || true
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' >/dev/null || true
		deb-systemd-helper unmask 'swift-object-auditor.service' 'swift-object-reconstructor.service' 'swift-object-replicator.service' >/dev/null || true
	fi
fi
# End automatically added section
# Automatically added by dh_systemd_enable/11.3.5
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask 'swift-object-auditor.service' 'swift-object-reconstructor.service' >/dev/null || true
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'swift-object-auditor.service' 'swift-object-reconstructor.service' >/dev/null || true
		deb-systemd-helper unmask 'swift-object-auditor.service' 'swift-object-reconstructor.service' >/dev/null || true
	fi
fi
# End automatically added section
# Automatically added by dh_systemd_enable/11.3.5
if [ "$1" = "remove" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper mask 'swift-object-auditor.service' >/dev/null || true
	fi
fi

if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'swift-object-auditor.service' >/dev/null || true
		deb-systemd-helper unmask 'swift-object-auditor.service' >/dev/null || true
	fi
fi
# End automatically added section
