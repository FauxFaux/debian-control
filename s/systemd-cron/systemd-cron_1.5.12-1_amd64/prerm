#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    deb-systemd-invoke stop cron-update.path
fi

# Automatically added by dh_systemd_start/10.10.9
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop 'cron.target' >/dev/null
fi
# End automatically added section

