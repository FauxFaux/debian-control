#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/tayga" ]; then
		update-rc.d tayga defaults >/dev/null
		invoke-rc.d tayga start || exit $?
	fi
fi
# End automatically added section
