#!/bin/sh
set -e
# Automatically added by dh_systemd_start/10.9
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	deb-systemd-invoke stop usbguard-dbus.service usbguard.service >/dev/null
fi
# End automatically added section
