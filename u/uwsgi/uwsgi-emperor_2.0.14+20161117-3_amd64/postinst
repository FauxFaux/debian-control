#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/uwsgi-emperor" ]; then
		update-rc.d uwsgi-emperor defaults >/dev/null
		invoke-rc.d uwsgi-emperor start || exit $?
	fi
fi
# End automatically added section
