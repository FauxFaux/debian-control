#!/bin/sh
set -e
# Automatically added by dh_installcatalogs
if [ "$1" = "purge" ]; then
	rm -f /etc/sgml/w3-dtd-mathml.cat.old
fi
# End automatically added section
# Automatically added by dh_installxmlcatalogs
if [ "$1" = "purge" ]; then
	rm -f /etc/xml/w3-dtd-mathml.xml /etc/xml/w3-dtd-mathml.xml.old
fi
# End automatically added section
