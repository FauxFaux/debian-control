#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.5
if [ -x "/etc/init.d/watcher-decision-engine" ] && [ "$1" = remove ]; then
	invoke-rc.d watcher-decision-engine stop || true
fi
# End automatically added section
