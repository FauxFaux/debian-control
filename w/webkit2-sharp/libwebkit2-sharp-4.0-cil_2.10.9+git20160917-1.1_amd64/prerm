#!/bin/sh
set -e
# Automatically added by dh_installcligac/UNDECLARED
if [ "$1" = "remove" ] || [ "$1" = "upgrade" ] && [ -x /usr/share/cli-common/gac-package-remove ]; then
	/usr/share/cli-common/gac-package-remove libwebkit2-sharp-4.0-cil
fi
# End automatically added section
