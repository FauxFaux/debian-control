#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/whiteboard" ]; then
		update-rc.d whiteboard defaults >/dev/null
		invoke-rc.d whiteboard start || exit $?
	fi
fi
# End automatically added section
