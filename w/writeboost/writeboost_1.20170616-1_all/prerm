#!/bin/sh
set -e
# Automatically added by dh_installinit/11.3.4
if [ -x "/etc/init.d/writeboost" ] && [ "$1" = remove ]; then
	invoke-rc.d writeboost stop || exit 1
fi
# End automatically added section
