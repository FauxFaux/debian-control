#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/mbmon" ]; then
		update-rc.d mbmon defaults >/dev/null
		invoke-rc.d mbmon start || exit $?
	fi
fi
# End automatically added section
