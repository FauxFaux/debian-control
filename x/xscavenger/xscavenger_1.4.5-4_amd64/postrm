#! /bin/sh

set -e

# We exit unless the package is being removed or purged.
case "$1" in
	remove) ;;
	upgrade)		exit 0;;
	purge) ;;
	failed-upgrade)		exit 0;;
	abort-install)		exit 0;;
	abort-upgrade)		exit 0;;
	disappear)		exit 0;;
	*)			exit 0;;
esac
