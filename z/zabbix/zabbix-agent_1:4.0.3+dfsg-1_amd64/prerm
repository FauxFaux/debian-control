#!/bin/sh
set -e
# Automatically added by dh_installinit/11.5.4
if [ -x "/etc/init.d/zabbix-agent" ] && [ "$1" = remove ]; then
	invoke-rc.d zabbix-agent stop || exit 1
fi
# End automatically added section
