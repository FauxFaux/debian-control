#!/bin/sh
set -e
# Automatically added by dh_installdocs
if [ "$1" = configure ] && which install-docs >/dev/null 2>&1; then
	install-docs -i /usr/share/doc-base/zpb-ttf
fi
# End automatically added section
# Automatically added by dh_lisp
if [ "$1" = "configure" ] &&
   [ -x /usr/sbin/register-common-lisp-source ]; then
	/usr/sbin/register-common-lisp-source "zpb-ttf"
fi
# End automatically added section
